/**
* _abs - Obtains abs val of input num
*@n: type of expected input
*
* Return: Always 0 (success)
*/

#include "main.h"

int _abs(int n)
{

	if (n >= 0)
{		n = n;
		return (n);
}
	else
{		n = -n;
		return (-n);
}

}
