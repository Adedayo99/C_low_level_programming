/**
* is_lower - checks for lowercase characters
*
* Return: Always 1 (success)
*/

#include "main.h"

int is_lower(int c)
{
	int i;
	int flag;

	i = 'a';
	while (i <= 'z')
{
	if (c == i)
	flag = 1;
	return (1);
}

	if (flag == 1)
	return (0);

}
