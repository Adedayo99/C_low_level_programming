/**
* rev_string - string reversal f
*@s: input string
*
* Return: No return val
*/

#include "main.h"

void rev_string(char *s)
{
	int i;
	int count;
	char mychar[];

	for (i = 0; s[i] != '\0'; i++)
	{
	count += 1;
	}

	for (i = --count; i >= 0; i--)
	{
	mychar[i] = s[i];
	}

	s = mychar[];

}
