/**
* _strlen - counts string len
*@s: input char string pointer
*
*Return: An int value
*/

#include "main.h"

int _strlen(char *s)
{
	int i;
	int count;

	count = 0;


	for (i = 0; s[i] != '\0'; i++)
	}

		count += 1;
	}

	return (count);
}

